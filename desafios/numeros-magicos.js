// O código em que você vai praticar esses conceitos contém 
// funções que lidam com checagem de atualizações 
// de uma aplicação e cálculo de desconto, onde:

// 1. O tempo de checagem de uma atualização por meio do `setInterval` é feita a cada 30 minutos.
// 2. Os cálculos do desconto devem ter o preço em centavos e o desconto em porcentagem.

// Exemplo: numero_magico

function lookForUpdades() {}

const thirtyMinutes = 60 * 30 * 1000;

setInterval(lookForUpdades, thirtyMinutes)

function calculateDiscount(price, discount) {}